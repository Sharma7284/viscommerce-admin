import React, { useState } from "react";
import { ToastContainer, toast } from "react-toastify";
import "react-toastify/ReactToastify.css";

const UploadToastify = () => {
//   const [uploadedItems, setUploadedItems] = useState(0);

//   const handleUpload = (newItems = 10) => {
//     const totalItems = uploadedItems + newItems;
//     setUploadedItems(totalItems);
//     toast.success(
//       `${newItems} item(s) uploaded successfully! Total: ${totalItems} item(s).`
//     );
//   };

  return <ToastContainer />;
};

export default UploadToastify;
